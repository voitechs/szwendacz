Szwendacz map rollback v1.3

This patch keeps the recent non-map changes, but restores the previous stable map.

Keeps:
- flag icons (including Denmark alias and split flags)
- green Crown of Europe homepage card
- green climbed-peak rows
- no vertical stripe on climbed rows

Restores:
- previous europe-map.json SVG country map
- previous country/peak styling and map appearance

Removes dependencies:
- world-atlas
- topojson-client

Keeps dependency:
- flag-icons

Upload all files from this patch together.
