Szwendacz — peak coordinates patch v1.0

Replace:
  src/data/crown-of-europe.json

What changed:
- all 45 project records now contain `coordinates: [lat, lon]`
- `countryCenter` is also set to the same summit coordinates for compatibility
  with the current SVG map code, so no template change is required
- 44 records use Wikipedia coordinates
- Jurmysz has no dedicated English Wikipedia page; its coordinate uses a
  documented Crown of Europe fallback source and is clearly flagged in the audit
- each record contains `coordinateSource`

See COORDINATE_AUDIT.csv for old/new coordinates and source URLs.
